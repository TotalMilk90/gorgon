BEGIN h_prostd

IF ~RandomNum(8,8)~ THEN BEGIN 0
   SAY @53500 /* Well, they say a dance is just a vertical expression of a horizontal desire. */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,1)~ THEN BEGIN 1
   SAY @53501 /* Oh, you're quite the bold one, aren't you? I do hope you can keep up with my... pace. */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,2)~ THEN BEGIN 2
   SAY @53502 /* Ah, the night is young, and so are we. Let's make the most of it, shall we? */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,3)~ THEN BEGIN 3
   SAY @53503 /* You have a certain fire in your eyes. I wonder what other passions lie beneath the surface? */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,4)~ THEN BEGIN 4
   SAY @53504 /* You're a mystery, my dear. I wonder what secrets you hold, waiting to be discovered. */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,5)~ THEN BEGIN 5
   SAY @53505 /* They say that true connection happens in the spaces between words. Care to find out? */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,6)~ THEN BEGIN 6
   SAY @53506 /* A night like this calls for bold moves and daring hearts, wouldn't you agree? */
IF ~~ THEN EXIT
END

IF ~RandomNum(8,7)~ THEN BEGIN 7
   SAY @53507 /* There's a certain electricity between us, don't you think? */
IF ~~ THEN EXIT
END